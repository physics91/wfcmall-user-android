<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/my_coupon_list_graph"
    app:startDestination="@id/my_coupon_list_fragment">

    <fragment
        android:id="@+id/my_coupon_list_fragment"
        android:name="com.theone.busandbt.fragment.coupon.MyCouponListFragment"
        android:label="MyCouponListFragment">
        <argument
            android:name="serviceTypeId"
            app:argType="integer" />
        <argument
            android:name="deliveryTypeId"
            app:argType="integer" />
        <argument
            android:name="couponTypeId"
            android:defaultValue="0"
            app:argType="integer" />
    </fragment>
</navigation>